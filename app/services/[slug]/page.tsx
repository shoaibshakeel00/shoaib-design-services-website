import {notFound} from 'next/navigation';import Link from 'next/link';import {serviceMap} from '@/data/services';import TemplateGallery from '@/components/TemplateGallery';export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=serviceMap[slug];if(!s)notFound();return <main><section className="page-hero"><div className="container"><span className="eyebrow">{s.category}</span><h1 className="title">{s.title}</h1><p className="lead">{s.description}</p><div className="hero-actions"><Link className="btn btn-primary" href={`/order?service=${s.slug}`}>Order Now • Rs. {s.price.toLocaleString()}</Link><span className="tag">Delivery: {s.delivery}</span></div></div></section><section className="section" style={{paddingTop:25}}><div className="container"><div className="two-col"><div className="card"><h3>What’s included</h3><ul className="feature-list">{s.features.map(f=><li key={f}>{f}</li>)}</ul></div><div className="card"><h3>Online workflow</h3><p className="muted">Select the service, enter your requirements, upload documents and submit. We will contact you for clarification if needed.</p></div></div>{s.templates?.length?<div style={{marginTop:55}}><span className="eyebrow">Templates</span><h2 className="title">Choose your style.</h2><TemplateGallery serviceSlug={s.slug}/></div>:null}</div></section></main>}
