'use client';import {useState} from 'react';import {services} from '@/data/services';import ServiceCard from '@/components/ServiceCard';export default function Services(){const [q,setQ]=useState('');const [cat,setCat]=useState('All');const cats=['All','Documents','Jobs','Education','Computer','Design','Websites'];const list=services.filter(s=>(cat==='All'||s.category===cat)&&(`${s.title} ${s.description}`.toLowerCase().includes(q.toLowerCase())));return <main><section className="page-hero"><div className="container"><span className="eyebrow">Services</span><h1 className="title">Online services that get the job done.</h1><p className="lead">Professional digital assistance for documents, jobs, education, computers, designs and websites.</p></div></section><section className="section" style={{paddingTop:30}}><div className="container"><div className="two-col" style={{alignItems:'center',marginBottom:22}}><input className="search" placeholder="Search services…" value={q} onChange={e=>setQ(e.target.value)}/><div className="filter-row" style={{margin:0}}>{cats.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div></div><div className="cards">{list.map(s=><ServiceCard service={s} key={s.slug}/>)}</div></div></section></main>}
