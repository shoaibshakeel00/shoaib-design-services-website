'use client';import {useEffect,useState} from 'react';export default function Customers(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/orders').then(r=>r.json()).then(d=>{const m=new Map();(d.orders||[]).forEach((o:any)=>m.set(o.email,{name:o.customer_name,email:o.email,whatsapp:o.whatsapp,orders:(m.get(o.email)?.orders||0)+1}));setRows([...m.values()])})},[]);return <><h1 className="title">Customers</h1><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>WhatsApp</th><th>Orders</th></tr></thead><tbody>{rows.map(x=><tr key={x.email}><td>{x.name}</td><td>{x.email}</td><td>{x.whatsapp}</td><td>{x.orders}</td></tr>)}</tbody></table></div></>}
