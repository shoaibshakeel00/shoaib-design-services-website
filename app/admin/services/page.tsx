import {services} from '@/data/services';export default function AdminServices(){return <><h1 className="title">Services</h1><p className="muted">Your service catalog is centralized in <code>data/services.ts</code>, so prices and service details can be changed without editing page components.</p><div className="table-wrap"><table className="table"><thead><tr><th>Service</th><th>Category</th><th>Starting price</th><th>Delivery</th></tr></thead><tbody>{services.map(s=><tr key={s.slug}><td>{s.title}</td><td>{s.category}</td><td>Rs. {s.price.toLocaleString()}</td><td>{s.delivery}</td></tr>)}</tbody></table></div></>}
