'use client';import {useEffect,useState} from 'react';export default function Settings(){const [f,setF]=useState<any>({business_name:'Shoaib Digital Services',email:'shoaibportfolio1@gmail.com',whatsapp:'',facebook:'',instagram:'',linkedin:'',website_url:'',address:'',currency:'PKR'});const load=()=>fetch('/api/admin/settings').then(r=>r.json()).then(d=>setF((x:any)=>({...x,...(d.settings && typeof d.settings === 'object' ? d.settings : {})})));useEffect(() => { load(); }, []);const save=async()=>{await fetch('/api/admin/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});alert('Settings saved.')};return <><h1 className="title">Settings</h1><div className="form-shell" style={{margin:0}}><div className="form-grid">{Object.keys(f).map(k=><div className="field" key={k}><label>{k.replaceAll('_',' ')}</label><input value={f[k]||''} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}</div><button className="btn btn-primary" onClick={save}>Save Settings</button></div></>}
