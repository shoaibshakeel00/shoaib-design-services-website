'use client';import {useEffect,useState} from 'react';export default function Messages(){const [m,setM]=useState<any[]>([]);const load=()=>fetch('/api/admin/messages').then(r=>r.json()).then(d=>setM(d.messages||[]));useEffect(() => {
  load();
}, []);const read=async(id:string)=>{await fetch('/api/admin/messages',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()};return <><h1 className="title">Contact Messages</h1><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Contact</th><th>Service</th><th>Message</th><th>Read</th></tr></thead><tbody>{m.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.email}<br/>{x.whatsapp}</td><td>{x.service}</td><td>{x.message}</td><td><button className="btn btn-ghost" onClick={()=>read(x.id)}>{x.read?'Read':'Mark read'}</button></td></tr>)}</tbody></table></div></>}
