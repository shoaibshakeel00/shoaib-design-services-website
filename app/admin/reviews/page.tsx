'use client';import {useEffect,useState} from 'react';export default function ReviewsAdmin(){const [items,setItems]=useState<any[]>([]);const load=()=>fetch('/api/admin/reviews').then(r=>r.json()).then(d=>setItems(d.reviews||[]));useEffect(() => {
  load();
}, []);const act=async(id:string,action:string)=>{await fetch('/api/admin/reviews',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,action})});load()};return <><h1 className="title">Reviews</h1><div className="table-wrap"><table className="table"><thead><tr><th>Name</th><th>Rating</th><th>Review</th><th>Approved</th><th/></tr></thead><tbody>{items.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.rating}/5</td><td>{r.review}</td><td>{r.approved?'Yes':'No'}</td><td><button className="btn btn-ghost" onClick={()=>act(r.id,r.approved?'hide':'approve')}>{r.approved?'Hide':'Approve'}</button><button className="btn btn-ghost" onClick={()=>act(r.id,'delete')}>Delete</button></td></tr>)}</tbody></table></div></>}
