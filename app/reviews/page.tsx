'use client';import {useEffect,useState} from 'react';import {Star} from 'lucide-react';export default function Reviews(){const [reviews,setReviews]=useState<any[]>([]);const [f,setF]=useState({name:'',rating:5,review:''});const [done,setDone]=useState(false);useEffect(()=>{fetch('/api/reviews').then(r=>r.json()).then(d=>setReviews(d.reviews||[]))},[]);const submit=async(e:any)=>{e.preventDefault();const r=await fetch('/api/reviews',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(r.ok){setDone(true);setF({name:'',rating:5,review:''})}};return <main><section className="page-hero"><div className="container"><span className="eyebrow">Reviews</span><h1 className="title">Real feedback, shared with approval.</h1><p className="lead">Customers can submit a review. It appears publicly after admin approval.</p></div></section><section className="section" style={{paddingTop:20}}><div className="container"><div className="cards">{reviews.length?reviews.map(r=><div className="card" key={r.id}><div>{Array.from({length:r.rating}).map((_,i)=><Star key={i} size={16} fill="currentColor" style={{display:'inline'}}/>)}</div><p>“{r.review}”</p><b>{r.name}</b></div>):<div className="card"><p className="muted">No approved reviews yet. Be the first.</p></div>}</div><div className="form-shell"><span className="eyebrow">Share feedback</span><h2 className="title">Leave a review</h2>{done?<div className="success">Thank you. Your review has been submitted for approval.</div>:<form onSubmit={submit}><div className="form-grid"><div className="field"><label>Name *</label><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></div><div className="field"><label>Rating</label><select value={f.rating} onChange={e=>setF({...f,rating:Number(e.target.value)})}>{[5,4,3,2,1].map(n=><option key={n} value={n}>{n} stars</option>)}</select></div><div className="field full"><label>Review *</label><textarea required value={f.review} onChange={e=>setF({...f,review:e.target.value})}/></div></div><button className="btn btn-primary">Submit Review</button></form>}</div></div></section></main>}
