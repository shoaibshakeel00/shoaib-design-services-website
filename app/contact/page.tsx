'use client';import {useState} from 'react';import {Mail,MessageCircle,Send} from 'lucide-react';import {siteConfig,whatsappLink} from '@/data/siteConfig';export default function Contact(){const [f,setF]=useState({name:'',whatsapp:'',email:'',service:'',message:''});const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);const u=(k:string,v:string)=>setF(x=>({...x,[k]:v}));const submit=async(e:any)=>{e.preventDefault();setLoading(true);try{const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});if(!r.ok)throw new Error();setDone(true)}catch{alert('We could not send your message. Please try WhatsApp.')}finally{setLoading(false)}};return <main><section className="page-hero"><div className="container"><span className="eyebrow">Contact</span><h1 className="title">Let’s get your request moving.</h1><p className="lead">Send a quick inquiry or start a service request online.</p></div></section><section className="section" style={{paddingTop:20}}><div className="container contact-grid"><div className="info-stack"><div className="info-box"><Mail/><h3>Email</h3><a className="muted" href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a></div><div className="info-box"><MessageCircle/><h3>WhatsApp</h3><a className="muted" href={whatsappLink()} target="_blank">Chat with Shoaib</a></div><div className="info-box"><h3>Online service</h3><p className="muted">Send your documents from anywhere. Use the order form for a structured request.</p></div></div><div className="form-shell" style={{margin:0}}>{done?<div className="success"><h2>Thank you!</h2><p className="muted">Your request has been received. We will contact you shortly.</p></div>:<form onSubmit={submit}><div className="form-grid"><div className="field"><label>Name *</label><input required value={f.name} onChange={e=>u('name',e.target.value)}/></div><div className="field"><label>WhatsApp</label><input value={f.whatsapp} onChange={e=>u('whatsapp',e.target.value)}/></div><div className="field"><label>Email *</label><input required type="email" value={f.email} onChange={e=>u('email',e.target.value)}/></div><div className="field"><label>Service</label><input value={f.service} onChange={e=>u('service',e.target.value)} placeholder="e.g. CV design"/></div><div className="field full"><label>Message *</label><textarea required value={f.message} onChange={e=>u('message',e.target.value)}/></div></div><button disabled={loading} className="btn btn-primary" type="submit">{loading?'Sending…':'Send Message'} <Send size={16}/></button></form>}</div></div></section></main>}
