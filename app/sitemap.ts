import type {MetadataRoute} from 'next';import {siteConfig} from '@/data/siteConfig';import {services} from '@/data/services';export default function sitemap():MetadataRoute.Sitemap{const base=siteConfig.url;return ['','services','templates','pricing','how-it-works','jobs','reviews','about','contact','track','order','documents',...services.map(s=>`services/${s.slug}`)].map(path=>({url:`${base}/${path}`,lastModified:new Date()}))}
