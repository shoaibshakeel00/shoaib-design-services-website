import {NextResponse} from 'next/server';import {createAdminClient} from '@/lib/supabase/admin';export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.email||!b.message)return NextResponse.json({error:'Missing fields'},{status:400});const {error}=await createAdminClient().from('contact_messages').insert({name:String(b.name).slice(0,120),whatsapp:String(b.whatsapp||'').slice(0,40),email:String(b.email).slice(0,160),service:String(b.service||'').slice(0,120),message:String(b.message).slice(0,4000)});if(error)throw error;return NextResponse.json({ok:true});}catch{ return NextResponse.json({error:'Unable to send message'},{status:500})}}
